# MPC Test Configuration File
# You can configure various combinations of MPC tests.

# Test configuration list
# Each test can have the following options:
# Environment type (options: "s_path", "straight_corridor", "maze", "oblique_maze", "parking")
# Robot shape (options: "rectangle", "pentagon", "triangle", "lshape")
# Optimizer type (options: "obca", "psdf", "mpcc", "rmpcc", "rmpcc_pv", "acados", "casadi", "dcbf", "dcbf_casadi")
# Dynamics model (currently only "differential_drive" is supported)
# Path planner type (options: "astar", "hybrid_astar")

# True initial-pose randomization (independent of localization/measurement noise).
# CLI: --perturb-start / --no-perturb-start; explicit CLI values override YAML.
# Use the same seed for psdf/dcbf/obca, and change it for each trial.
# A null seed is generated and recorded; methods within a config batch share it.
# Individual test_configs entries may override this block (e.g. seed: 1, 2, ...).
start_perturbation:
  enabled: false
  seed: null
  position_std: 0.005       # Independent x/y standard deviation [m]: 5 mm.
  heading_std_deg: 0.0     # Keep heading fixed; set > 0 to also perturb yaw.
  max_sigma: 3.0          # Rejection truncation: |dx|, |dy| <= 15 mm by default.
  min_clearance: 0.01     # Whole footprint to obstacles/map bounds [m].
  max_attempts: 1000      # Fail explicitly if no safe sample can be found.

test_configs:
  - maze_type: "maze"
    robot_shape: "rectangle"
    optimizer_type: "rmpcc"
    dynamics_type: "differential_drive"
    path_planner: "astar"

  - maze_type: "s_path"
    robot_shape: "rectangle"
    optimizer_type: "rmpcc"
    dynamics_type: "differential_drive"
    path_planner: "astar"

  # - maze_type: "s_path"
  #   robot_shape: "rectangle"
  #   optimizer_type: "rmpcc_pv"
  #   dynamics_type: "differential_drive"
  #   path_planner: "astar"

  # - maze_type: "s_path"
  #   robot_shape: "rectangle"
  #   optimizer_type: "mpcc"
  #   dynamics_type: "differential_drive"
  #   path_planner: "astar"

  # - maze_type: "s_path"
  #   robot_shape: "rectangle"
  #   optimizer_type: "psdf"
  #   dynamics_type: "differential_drive"
  #   path_planner: "astar"

  # - maze_type: "straight_corridor"
  #   robot_shape: "rectangle"
  #   optimizer_type: "rmpcc"
  #   dynamics_type: "differential_drive"
  #   path_planner: "astar"

  # - maze_type: "straight_corridor"
  #   robot_shape: "rectangle"
  #   optimizer_type: "rmpcc_pv"
  #   dynamics_type: "differential_drive"
  #   path_planner: "astar"

  # - maze_type: "straight_corridor"
  #   robot_shape: "rectangle"
  #   optimizer_type: "mpcc"
  #   dynamics_type: "differential_drive"
  #   path_planner: "astar"

  # - maze_type: "straight_corridor"
  #   robot_shape: "rectangle"
  #   optimizer_type: "psdf"
  #   dynamics_type: "differential_drive"
  #   path_planner: "astar"

  # - maze_type: "maze"
  #   robot_shape: "rectangle"
  #   optimizer_type: "rmpcc"
  #   dynamics_type: "differential_drive"
  #   path_planner: "astar"

  # - maze_type: "maze"
  #   robot_shape: "rectangle"
  #   optimizer_type: "rmpcc_pv"
  #   dynamics_type: "differential_drive"
  #   path_planner: "astar"

  # - maze_type: "maze"
  #   robot_shape: "rectangle"
  #   optimizer_type: "mpcc"
  #   dynamics_type: "differential_drive"
  #   path_planner: "astar"

  # - maze_type: "maze"
  #   robot_shape: "rectangle"
  #   optimizer_type: "psdf"
  #   dynamics_type: "differential_drive"
  #   path_planner: "astar"

  # - maze_type: "oblique_maze"
  #   robot_shape: "rectangle"
  #   optimizer_type: "rmpcc"
  #   dynamics_type: "differential_drive"
  #   path_planner: "astar"

  # - maze_type: "oblique_maze"
  #   robot_shape: "rectangle"
  #   optimizer_type: "rmpcc_pv"
  #   dynamics_type: "differential_drive"
  #   path_planner: "astar"

  # - maze_type: "oblique_maze"
  #   robot_shape: "rectangle"
  #   optimizer_type: "mpcc"
  #   dynamics_type: "differential_drive"
  #   path_planner: "astar"

  # - maze_type: "oblique_maze"
  #   robot_shape: "rectangle"
  #   optimizer_type: "psdf"
  #   dynamics_type: "differential_drive"
  #   path_planner: "astar"

# Default settings
# Common settings applied to all tests
defaults:
  frame_skip: 2                # Animation frame skip (1=every frame, 5=every 5th frame)
  simulation_time: 20.0         # Simulation time (seconds)
  generate_animation: true      # Whether to generate animation (true/false)
  generate_plots: true          # Whether to generate static plots (true/false)
  speed_range: [0.0, 0.30]      # Shared color scale for speed heatmaps [m/s]
  clearance_range: [0.00, 0.03] # Shared color scale for clearance/SDF heatmaps [m]
  risk_range: [0.0, 0.045]     # Shared color scale for risk heatmaps [m]

# =====================================================
# Option descriptions:
# =====================================================
#
# maze_type options:
#   - "s_path": Simple S-shaped path environment
#   - "straight_corridor": Straight path with a narrow corridor in the middle
#   - "maze": Complex maze environment  
#   - "oblique_maze": Maze with tilted obstacles
#   - "parking": Parking scenario environment
#
# robot_shape options:
#   - "rectangle": Rectangular robot (0.15x0.09m)
#   - "pentagon": Pentagon robot (radius 0.05m)
#   - "triangle": Triangle robot
#   - "lshape": L-shaped robot
#
# optimizer_type options:
#   - "psdf": PSDF-based SQP optimizer (obstacle avoidance)
#   - "mpcc": MPCC optimizer with PSDF safety constraint
#   - "rmpcc": MPCC with fixed signed-PSDF guard and stage-masked MF affine rows
#   - "rmpcc_pv": Risk-aware MPCC optimizer with a projected-variance PSDF margin
#   - "obca": OBCAOptimizer from control/obca_optimizer.py (CasADi/IPOPT)
#   - "acados": ACADOS SQP-based NMPC
#   - "casadi": CasADi-based basic NMPC  
#   - "dcbf": DCBF-based SQP optimizer
#   - "dcbf_casadi": DCBF-based CasADi optimizer
#
# dynamics_type options:
#   - "differential_drive": Differential drive robot model (currently the only option)
#
# path_planner options:
#   - "astar": Traditional A* with Line of Sight path reduction
#   - "hybrid_astar": Hybrid A* considering vehicle kinematics
# =====================================================
